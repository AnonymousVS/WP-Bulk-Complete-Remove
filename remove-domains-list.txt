# =============================================================
# remove-domains-list.txt
# รายชื่อ domain ที่ต้องการลบ WordPress แบบ completely
#
# วิธีใช้:
#   1) เพิ่มชื่อ domain บรรทัดละ 1 domain
#   2) Commit Changes บน GitHub
#   3) SSH เข้าเซิร์ฟเวอร์ แล้วรัน: wp-bulk-complete-remove.sh
#
# หมายเหตุ:
#   - บรรทัดที่ขึ้นต้นด้วย # คือ comment (ไม่ถูกลบ)
#   - บรรทัดว่างจะถูกข้าม
#   - ใส่แค่ชื่อ domain ไม่ต้องมี http:// หรือ /
# =============================================================

# ตัวอย่าง (ลบ # ข้างหน้าออกแล้วใส่ domain จริง):
# example1.com
# example2.co
# example3.net
